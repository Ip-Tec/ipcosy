{
  "name": "ipcosy-monorepo",
  "version": "0.0.0",
  "private": true,
  "packageManager": "pnpm@10.27.0",
  "scripts": {
    "dev": "pnpm -r dev",
    "build": "pnpm -r build",
    "lint": "pnpm -r lint",
    "postinstall": "pnpm -r db:generate || true"
  },
  "devDependencies": {}
}
